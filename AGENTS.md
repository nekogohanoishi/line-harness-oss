# Project Instructions

- ゴールから外れる提案をしないでください。
- ゴールに進む提案を必ずしてください。
- 回答には必ず「次のタスクはこれ」「今の進捗を全体像から整理するとこれ」を含めてください。
- 私が大学生だと思って、言語化してください。

## 作業メモ

- LINEメッセージの現行設定をNotionへ記録するときは、`公式LINE` 配下ではなく `00_Home` 直下のページに置いてください。
- 本番Workerの一時デプロイ設定を作るときは、`.line-harness-setup.json` の `workerUrl` をそのまま信用しないでください。`apps/web/.env.production` の `NEXT_PUBLIC_API_URL`、公開DNS、HTTP応答を照合し、管理画面が実際に利用している到達可能なURLを `WORKER_URL` に設定してください。
- `WORKER_URL` と `NEXT_PUBLIC_API_URL` が異なる場合は、理由とカスタムドメインの稼働を確認できるまでデプロイしないでください。
- LINEの受信Webhookを変更するときは、Harness導入前で`friends`未登録の友だちからのテキスト・非テキスト・postbackを回帰テストし、受信時の自動登録では`friend_add`シナリオを起動しないでください。
- 本番Workerの`wrangler deploy`が`dist/line_harness`を参照する構成では、最後のWorkerソース修正後に必ず`pnpm --filter worker build`を再実行してからデプロイしてください。デプロイ出力のbundle名または更新時刻が直前ビルドと一致することまで確認してください。
- Cloudflare Pagesの本番Admin UIをCLIからデプロイするときは、現在のGitブランチへ暗黙に依存させず、`wrangler pages deploy ... --branch main`のように本番ブランチを明示してください。完了後はプレビューURLだけでなく正規の`pages.dev` URLから新しい生成物を読み戻してください。
- LINEメッセージ本文を「そのまま」「名前だけ変更」等の指定で反映するときは、改行・句読点・空白・URL・日程表現を変更せず、指定された置換だけを行ってください。反映前後に元本文との差分を機械的に確認し、許可された置換以外の差分が0件であることを確認してください。
- 新規友だち追加・ブロック解除時の配信を変更するときは、本文を編集した対象だけで完了とせず、本番の有効な `friend_add` シナリオを全件確認してください。意図したシナリオだけが有効で、旧シナリオや全アカウント共通シナリオが並行配信されないことを、変更後の本番APIから読み戻して確認してください。
- 管理画面へのディープリンクを生成するときは、URLが要求するIDの種類（例: event ID、booking ID）を呼び出し元の変数名とAPIルートで照合してください。生成関数の単体テストだけでなく、呼び出し側が正しい種類のIDを渡していることも読み戻して確認してください。
- 新規友だち追加・ブロック解除のあいさつ本文を変更するときは、登録時アンケートの有効・無効を明示指示なしに変更しないでください。指定本文がアンケート用シナリオ内にある場合も、既存のアンケート状態を保存し、本文変更とアンケート再開を同一操作として扱わないでください。
- 「登録時アンケートを複数作る」という要望を、通常アンケートが複数作れることと同一視しないでください。登録時用の候補を複数保存できること、現在配信する候補を選択できること、複数同時配信か1件選択かを区別して要件と本番挙動を確認してください。
